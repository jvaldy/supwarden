nelmio_api_doc:
    documentation:
        openapi: 3.0.0
        info:
            title: Supwarden API
            description: Documentation REST OpenAPI de l'API Supwarden.
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: Token
    areas:
        default:
            path_patterns:
                - ^/api
